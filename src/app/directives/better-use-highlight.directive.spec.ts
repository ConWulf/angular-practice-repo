import { BetterUseHighlightDirective } from './better-use-highlight.directive';

describe('BetterUseHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterUseHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
